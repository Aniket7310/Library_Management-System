# Library_Management-System
The Library Management System is a Java Swing application that allows users to add, issue, return, and view books. It uses OOP concepts, multithreading, and exception handling for smooth functionality. Book records are stored and managed using MySQL with JDBC for database connectivity and CRUD operations.
